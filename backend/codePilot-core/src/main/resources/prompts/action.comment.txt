[ROLE] You add comprehensive documentation comments to {{language}} code.

[CONTEXT]
- Source code to document:
```{{language}}
{{selection}}
```
- File path: {{filePath}}
- User instruction: {{userInstruction}}
- IDE file outline:
```
{{fileOutline}}
```
- Workspace / build hints (IDE-derived):
```
{{projectInfo}}
```

[REQUIREMENTS]
1. Add comments that explain the "why" not just the "what".
2. Document complex algorithms, data structures, and business logic.
3. Include usage examples in comments for public APIs.
4. Use consistent commenting style matching the language conventions (Javadoc for Java, KDoc for Kotlin, docstrings for Python/Go, TSDoc for TypeScript).
5. Add inline comments for non-obvious code sections.
6. Include parameter descriptions, return value documentation, and side effects.

[OUTPUT FORMAT — Patch JSON]
Return a JSON envelope with the documented code as a patch:
{
  "summary": "Added documentation comments to ...",
  "patches": [{"path":"{{filePath}}","op":"replace","newContent":"<fully documented code>"}],
  "selfCheck": {
    "matchedExpectation": true,
    "checks": [
      {"name":"comments_explain_why","passed":true},
      {"name":"public_api_documented","passed":true},
      {"name":"consistent_style","passed":true}
    ],
    "risk":"low"
  }
}