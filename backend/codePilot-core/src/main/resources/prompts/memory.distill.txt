[ROLE] You are distilling persistent long-term memory items from this conversation. The user has explicitly opted in. Extract ONLY facts the user has confirmed AT LEAST ONCE through their messages or decisions — never invent.

[INPUT]
- User locale: {{userLocale}}
- Project root hash: {{projectRootHash}}
- Existing memories (will be merged; do NOT duplicate):
{{existingMemories}}
- Recent turns (most recent first, last 20):
{{recentTurns}}

[OUTPUT — strict JSON, no markdown fences]
{
  "add": [
    {
      "id": "<deterministic-id, e.g. 'stack/runtime/jdk21'>",
      "scope": "global|project:<hash>|session:<id>",
      "kind": "preference|fact|convention|alias|skill-hint",
      "text": "<one-line in {{userLocale}}>",
      "evidence": [
        {"turn": <int>, "quote": "<short user quote that proves it>"}
      ],
      "confidence": 0.0,
      "ttl": "session|days:30|days:90|permanent"
    }
  ],
  "update": [
    {"id": "<existing>", "text": "<new>", "reason": "<why update>"}
  ],
  "remove": [
    {"id": "<existing>", "reason": "<contradicted|outdated|user-asked-to-forget>"}
  ]
}

[RULES]
1. confidence: 0.9+ when the user stated it directly twice OR confirmed an option; 0.6-0.9 when stated once and unchallenged; <0.6 → DROP, don't emit.
2. NEVER store: passwords, tokens, real names, emails, phone numbers, paths
   outside the workspace, full file contents, large code snippets. Redact
   anything matching those patterns and drop the item if redaction kills the
   information.
3. Prefer SHORT facts ("uses pnpm, not npm", "calls themselves QA") over
   long narratives.
4. If a recent user message contradicts an existing memory, emit a `remove`
   for the old id and an `add` for the new fact (do NOT silently update).
5. NEVER auto-promote conversational style preferences to permanent memories
   without explicit "记住" / "remember" / "save" trigger.
6. NEVER store opinions about other people or projects.
7. evidence.quote MUST be a substring of the user's actual message in
   `recentTurns`.

[OUTPUT LANGUAGE]
- text, evidence.quote, update.reason, remove.reason in {{userLocale}}.
- id, scope, kind, ttl in English.
