You are planning a task. Based on the user's request below, produce a JSON plan.

User request: {{input}}

[REQUIRED OUTPUT FORMAT — strict JSON, no other text]
{
  "userPlan": {
    "goal": "<restate the user's goal in user's language>",
    "summary": "<one-line summary in user's language>",
    "steps": [
      {"id": "s1", "index": 1, "title": "<verb-object title in user's language>", "description": "<brief description in user's language>", "status": "in_progress"}
    ],
    "status": "in_progress"
  },
  "phases": [
    {"id": "p1", "title": "<phase title>", "intent": "code-change", "entry": [], "exit": [], "budget": {"attempts": 3}}
  ],
  "infoRequests": null
}

[RULES]
- SIMPLE REQUESTS (single-file creation, bug fix, algorithm implementation, code refactoring) MUST use exactly 1 phase. Do NOT decompose into multiple phases.
- Only decompose into 2-5 phases when the task genuinely requires modifying 3+ files or spans multiple independent modules.
- Each phase should produce a concrete, verifiable code change.
- If you need more information before planning, set infoRequests to an array of structured requests instead:
  [{"id": "unique-id", "kind": "fs.read|code.outline|code.symbol|code.usages|ide.diagnostics", "args": {"path": "..."}}]
  - kind options: fs.read (read file content), code.outline (file structure), code.symbol (find symbol), code.usages (find references), ide.diagnostics (IDE errors/warnings)
- ALL user-facing strings (goal, summary, step titles/descriptions) MUST be in the SAME language as the user's request.
- If the user writes in Chinese, respond in Chinese. If in English, respond in English. Follow the user's language.
- User-facing strings follow {{userLocale}} (default zh-CN).
- NEVER create separate phases for "understanding", "testing", or "documentation" — these are part of the implementation phase.
- Prefer fewer phases with more comprehensive patches over many phases with tiny changes.